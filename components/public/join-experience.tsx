"use client";
import { useEffect,useState } from "react";import { Check,Copy,Gift,Share2,Sparkles,Users } from "lucide-react";
type Campaign={name:string;slug:string;description?:string;primaryColor:string;initialRewardTitle:string;initialRewardValue?:string;milestoneRewardTitle:string;qualifiedReferralGoal:number;page?:{headline:string;subheadline?:string;ctaLabel:string}};
export function JoinExperience({slug,ref}:{slug:string;ref?:string}){const [campaign,setCampaign]=useState<Campaign>();const [error,setError]=useState("");const [result,setResult]=useState<{participantId:string;referralCode:string;accessToken:string}>();const [form,setForm]=useState({name:"",email:"",phone:"",consent:false});useEffect(()=>{fetch(`/api/public/campaigns/${slug}`).then(r=>r.json()).then(v=>v.error?setError(v.error):setCampaign(v))},[slug]);async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch(`/api/public/campaigns/${slug}/join`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({...form,referralCode:ref})});const v=await r.json();if(!r.ok)return setError(v.error);setResult(v);await fetch(`/api/public/participants/${v.participantId}/reward-access`,{method:"POST",headers:{authorization:`Bearer ${v.accessToken}`}})}if(error)return <main className="public-error"><h1>Este loop não está disponível.</h1><p>{error}</p></main>;if(!campaign)return <main className="public-loading">Preparando sua experiência...</main>;const url=typeof window!=="undefined"?`${location.origin}/growth-loop/${slug}?ref=${result?.referralCode}`:"";return <main className="public-shell" style={{"--brand":campaign.primaryColor} as React.CSSProperties}><nav><span>GROWTH <b>LOOP</b></span><small>por Freitas Growth AI</small></nav>{!result?<><section className="public-hero"><span className="eyebrow"><Sparkles/> CONVITE ESPECIAL</span><h1>{campaign.page?.headline||campaign.name}</h1><p>{campaign.page?.subheadline||campaign.description}</p><div className="reward-promise"><Gift/><span><small>CADASTRE-SE E RECEBA</small><strong>{campaign.initialRewardTitle}</strong><em>{campaign.initialRewardValue}</em></span></div></section><form onSubmit={submit} className="join-card"><h2>Entre para o loop</h2><p>Leva menos de um minuto.</p><label>Seu nome<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Como podemos chamar você?"/></label><label>Seu melhor e-mail<input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="voce@empresa.com"/></label><label>WhatsApp <span>opcional</span><input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})} placeholder="(11) 99999-9999"/></label><label className="check"><input type="checkbox" checked={form.consent} onChange={e=>setForm({...form,consent:e.target.checked})}/><span>Li e concordo com os Termos e a Política de Privacidade.</span></label><button disabled={!form.consent}>{campaign.page?.ctaLabel||"Quero participar"}</button></form></>:<section className="success-card"><span className="success-icon"><Check/></span><span className="eyebrow">VOCÊ ESTÁ NO LOOP</span><h1>Primeira recompensa desbloqueada!</h1><div className="unlocked"><Gift/><span><small>SEU PRESENTE</small><strong>{campaign.initialRewardTitle}</strong><em>{campaign.initialRewardValue}</em></span></div><div className="next-step"><Users/><span><h2>Agora, convide {campaign.qualifiedReferralGoal} amigos</h2><p>Quando eles concluírem o cadastro e acessarem a primeira recompensa, você recebe <strong>{campaign.milestoneRewardTitle}</strong>.</p></span></div><label className="share-link"><input readOnly value={url}/><button onClick={()=>navigator.clipboard.writeText(url)}><Copy/></button></label><button className="share-button" onClick={()=>navigator.share?.({title:campaign.name,url})}><Share2/> Compartilhar convite</button></section>}</main>}
